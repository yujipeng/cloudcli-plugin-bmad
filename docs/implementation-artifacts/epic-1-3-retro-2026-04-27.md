# Sprint Retrospective — bmad-flow Epic 1-3

日期：2026-04-27
项目：bmad-flow
范围：Epic 1（发现与理解方法论）、Epic 2（执行与跨端连续体验）、Epic 3（信任、可达性与质量门禁）

## 交付概览

- Stories 完成：15/15（100%）
- 测试：19 通过 / 0 失败
- TypeScript strict：通过
- 新增源码模块：5（methodologyParser, methodologyViewModel, methodologyRender, interactionMode, methodologyState）
- 修改模块：3（types, server, index）

## Keep（做得好的）

1. 数据契约先行：Story 1.1 先定义类型，后续 story 在稳定契约上构建
2. Code review 早期发现 bug：skill 字段 fallback 和 BOM 处理在 Story 1.1 review 阶段修复
3. 依赖顺序清晰：parser → API → render → interaction → 错误处理 → 测试门禁
4. 静默降级设计：methodology 功能失败不影响 V1 核心功能

## Change（可以改进的）

1. Story 文件仅为 1.1 完整生成，后续 story 缺少独立的 create-story 产物分析
2. 测试集中在服务端逻辑层，前端 DOM 交互缺少自动化测试
3. renderMethodologySkeleton 已导出但未在 loading 分支启用

## Action Items

| # | 行动项 | 优先级 |
|---|--------|--------|
| 1 | 补充前端 DOM 交互测试（tooltip、折叠、复制反馈） | 高 |
| 2 | 在 load() loading 分支启用 renderMethodologySkeleton | 中 |
| 3 | 为 /methodology 端点补充 skippedRows/partialFailure 统计 | 中 |
| 4 | 搭建 Playwright E2E 测试基础设施 | 低 |

## 技术债务

1. parseCsvLine 不处理引号内换行符（低风险）
2. MethodologyItem.phase 是裸 string 而非受约束类型
3. renderMethodologySkeleton 未在 loading 分支集成

## 结论

Sprint 目标全部达成。methodology 导航功能从数据层到交互层完整交付，代码质量良好，测试覆盖核心逻辑。建议后续优先补充前端交互测试。
